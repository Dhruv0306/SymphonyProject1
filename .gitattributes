# Git LFS (Large File Storage) configuration for PyTorch model files
# This line configures Git LFS to track all .pt files in any directory
# .pt files are typically PyTorch model/checkpoint files that can be large in size
# Using Git LFS helps manage large binary files more efficiently in Git repositories
**/*.pt filter=lfs diff=lfs merge=lfs -text
